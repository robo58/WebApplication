@model Modeli

@{
    ViewBag.Title = "Ažuriranje usluge";
}

<form asp-route-page="@ViewBag.Page"
      asp-route-sort="@ViewBag.Sort"
      asp-route-ascending="@ViewBag.Ascending"
      method="post">
    <input type="hidden" asp-for="IdModela" />
    <div asp-validation-summary="All"></div>
    <div class="form-group">
        <label asp-for="Naziv"></label>
        <input asp-for="Naziv" class="form-control"/>
    </div>
    <div class="form-group">
        <label asp-for="IdSpecifikacija"></label>
        <div><span asp-validation-for="IdSpecifikacija" class="text-danger"></span></div>
        <select class="form-control" asp-for="IdSpecifikacija" asp-items="ViewBag.Specifikacije">
            <option disabled selected value="">Odaberite specifikaciju</option>
        </select>
    </div>
    <div class="text-center">
        <button class="btn btn-primary" type="submit">Spremi</button>
        <a asp-action="Index"
           asp-route-page="@ViewBag.Page"
           asp-route-sort="@ViewBag.Sort"
           asp-route-ascending="@ViewBag.Ascending"
           class="btn btn-secondary">Odustani</a>
    </div>
</form>